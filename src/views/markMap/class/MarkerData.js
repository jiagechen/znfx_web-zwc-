export default class MarkerData {
  constructor(geometry, geometryObject, marker, projectNo, remark) {
    this.geometry = geometry
    this.geometryObject = geometryObject
    this.marker = marker
    this.projectNo = projectNo
    this.remark = remark
  }
}
